config {
  type: "table"
}

-- Simulates raw ingest view over the LIFT data for financial transactions
 SELECT
    TRIM(Lonnsgruppe) AS Lonnsgruppe,
    TRIM(Tekst) as Tekst,
    TRIM(Utdanning) AS Utdanning,
    CURRENT_TIMESTAMP() AS ingestion_timestamp
    FROM
    `data_wh_dev_datapuls_cloud.raw_ref_lonnsgruppe_utdanning`